# Copy this to `.env` for local runs (uncommitted).
# The Python script reads these env vars.

# Required for local tests:
REPO_DIR=/absolute/path/to/your/local/target-repo

# Optional filters:
AUTHOR_EMAIL=
IGNORE_MERGES=1
LOOKBACK_DAYS=365

# Output path (local preview):
OUTPUT_SVG=public/heatmap.svg

# Not used locally (CI uses this), but you can note it here:
TARGET_REPO=owner/name